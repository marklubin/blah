# Session Journal - 2026-03-04-01

## Summary
Reworked the radar pipeline's triage scoring and source configuration to address 2 weeks of noisy results (491/626 signals from HN alone, quantized scores). Raised triage threshold, rewrote the scoring prompt with explicit anchoring, added per-source fetch limits, created 13 Discord channel sources, and cleaned up duplicate/overweight sources.

## Decisions Made
- Raised triage threshold from 0.3 to 0.5 — the old threshold let everything through
- Rewrote scoring prompt with explicit 0.0-1.0 tier descriptions and instruction to use continuous values (not quantized 0.3/0.5/0.9/1.0)
- Added platform label to each triage item so model has source context
- Per-source `max_items` stored in source config JSON rather than a new DB column — simpler, no migration needed
- Default limits: frontpage=15, timeline=30, search=15, account=20, subreddit=20, channel=50
- Set HN frontpage to 15, HN search to 10 — these were the noisiest sources
- Selected 13 Discord channels across 7 AI/memory-focused servers for monitoring
- Deferred Reddit and LinkedIn browser adapters to a separate session

## Changes
- `src/blah/services/triage.py` — threshold 0.3→0.5, rewritten prompt
- `src/blah/services/pipeline.py` — `_DEFAULT_LIMITS` class var, `fetch_limit` from config
- DB on salinas: deleted duplicate Twitter source, set HN limits, created 13 Discord sources

## Open Questions
- Discord channel count (13) may be too many — watch for noise in next few runs
- Score distribution still shows some clustering at round values (0.10, 0.15, 0.25, 0.35, 0.45) — model may need temperature tuning
- Reddit and LinkedIn need browser-based adapters (CDP scraping) — significant new code

## Next Steps
- Monitor next few pipeline runs to validate triage quality
- Build Reddit browser adapter (CDP via MCP router)
- Build LinkedIn browser adapter (same approach)
- Consider reducing Discord channel count if noise is too high
